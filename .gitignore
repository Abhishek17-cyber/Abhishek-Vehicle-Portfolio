# ── Environment variables (NEVER commit these) ──
.env
.env.local
.env.production

# ── Node modules ──
node_modules/
backend/node_modules/

# ── Uploaded files (stored on EC2, not in repo) ──
backend/uploads/
frontend/images/truck.jpg

# ── OS files ──
.DS_Store
Thumbs.db
desktop.ini

# ── Editor files ──
.vscode/
*.swp
*.swo

# ── Logs ──
*.log
npm-debug.log*

# ── PM2 ──
.pm2/
